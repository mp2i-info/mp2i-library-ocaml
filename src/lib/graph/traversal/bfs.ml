(* open Mp2i__Queue
open Mp2i__Graph

module Bfs (G : Graph) (Q : ImpQueue) = struct
	let traversal g r =
    let pred = Array.make 
		let q = Q.empty () in
		Q.push r q;
		while not Q.is_empty () do
       *)